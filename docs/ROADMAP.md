# ESP32-S3 嵌入式开发 TODO

依据：

- 当前硬件以 [`hardware/schematics/robot_controller_2026-07-31.svg`](../hardware/schematics/robot_controller_2026-07-31.svg) 为准。
- 软件结构参考 [`reference/legacy_embedded`](../reference/legacy_embedded) 精选旧工程的模块化、通信、诊断和安全设计。
- 旧工程是 ESP32 + STM32 + 多体节架构；当前项目是单块 ESP32-S3 直接控制双路直流电机、一个舵机和 MPU6050，因此迁移功能与设计思想，不直接复制 STM32 外设代码。

## 已完成

- [x] 建立 ESP-IDF 5.2.6 工程，目标芯片为 ESP32-S3。
- [x] 按 2026-07-31 SVG 建立统一引脚表。
- [x] DRV8833 双路 H 桥 PWM 驱动：nSLEEP、nFAULT、AIN1/AIN2、BIN1/BIN2。
- [x] 确认 GPIO46 在 ESP32-S3 上可作为 PWM 输出，不屏蔽电机驱动。
- [x] 舵机 50 Hz PWM 驱动和角度限幅。
- [x] MPU6050 I2C 初始化、WHO_AM_I 检查和原始数据读取。
- [x] GPIO35 -> 2N7002 外部低有效接口驱动，上电默认释放。
- [x] Wi-Fi SoftAP、HTTP 控制接口和浏览器控制页。
- [x] Python 图形上位机基础控制程序。
- [x] 串口调试命令：电机、舵机、I2C 扫描、IMU 和 GPIO35 控制。
- [x] 1 秒电机通信看门狗，控制中断后自动停车。
- [x] 2026-08-01 Debug/Release 独立干净构建成功：Debug `0xCE400`（剩余 19%），Release `0xAAD40`（剩余 33%）。
- [x] 2026-08-01 旧 PCB 无运动冒烟测试通过：烧录、启动、串口、FreeRTOS、Wi-Fi 关联、ARM 门禁和 MPU6050 100 Hz；详见 [`HARDWARE_SMOKE_TEST_2026-08-01.md`](HARDWARE_SMOKE_TEST_2026-08-01.md)。
- [x] 将 `app_main.c` 拆分为状态、执行器、控制、IMU、串口和启动编排模块。
- [x] 增加局域网自动发现与一键上位机启动器，兼容电脑同时连接有线网和手机热点。
- [x] 增加默认无运动的 HTTP 冒烟工具；运动测试需要 `--allow-motion`、指定单一阶段和人工确认。
- [x] STA 断线增加原因诊断以及 2/4/8/16/30 秒有界退避，避免连续扫描压制救援 AP。
- [x] 2026-08-03 最小系统板验证：COM8 烧录、APSTA、手机热点 DHCP、互联网并存、HTTP v1、安全状态转换和自动发现通过。

## P0：接板前完成的软件基础

详细设计见 [`PRE_HARDWARE_SOFTWARE_FOUNDATION.md`](PRE_HARDWARE_SOFTWARE_FOUNDATION.md)，逐项证据、命令和判定规则见 [`P0_ACCEPTANCE_MATRIX.md`](P0_ACCEPTANCE_MATRIX.md)。

- [x] 定义并冻结统一的控制协议版本。
  - 当前 HTTP API 继续保留，作为手机和桌面 GUI 的主接口。
  - 明确电机、转向、停止、舵机、GPIO35、状态查询和错误返回字段。
  - 旧工程的六字节帧 `FF FA mode parameter 88 77` 仅作为兼容层；先解决旧文件中模式编号含义不一致的问题。
- [x] 建立控制状态机：`BOOT -> SAFE -> READY -> RUNNING -> FAULT`，另含锁存 `ESTOP`。
- [x] 所有执行器上电保持安全状态：电机停止、DRV8833 休眠、舵机不输出、GPIO35 释放；显式 ARM 后才能运动。
- [x] 把 HTTP 回调与硬件驱动解耦：网络层只投递命令，独立执行器任务串行执行命令。
- [x] 建立统一命令队列，拒绝过期命令并记录队列溢出次数。
- [x] 实现真正的双电机独立控制：Motor A、Motor B 分别给定 `-100..100`。
- [x] 增加差速混控接口：`throttle + steering -> left/right`，为上位机摇杆和 Xbox 手柄做准备。
- [x] 电机增加斜坡限速、方向切换死区和立即急停，避免正反转瞬间冲击。
- [x] 读取并上报 DRV8833 nFAULT；故障时立即停车、拉低 nSLEEP，并保存故障原因。
- [x] 舵机增加可配置中位、最小/最大脉宽、角度限幅和变化速率限制。
- [x] 建立统一状态快照：运行状态、电机指令/输出、舵机角度、电源控制、IMU、nFAULT、Wi-Fi、运行时间和错误计数。

## P1：IMU、通信和诊断

- [ ] 使用 GPIO8 的 MPU6050 DATA_READY 中断，不再只靠手动命令读取。
- [ ] 固定频率 IMU 采样任务已完成；继续增加 I2C 恢复和明确丢帧计数。
- [ ] 将原始值转换为 `g`、`deg/s` 和摄氏度。
- [ ] 增加静止零偏校准、低通滤波和校准参数 NVS 保存。
- [x] 状态接口增加 IMU 最新原始值、有效标志、样本/错误计数和最后更新时间。
- [ ] 设备信息接口已有 API 版本、目标芯片和编译时间；继续增加固件版本、复位原因和芯片详细信息。
- [x] Wi-Fi 使用 APSTA：优先通过 STA 接入局域网，同时保留救援 SoftAP。
- [x] STA 断线自动重连；HTTP 服务不依赖 STA 生命周期，不因普通断线重启整机。
- [ ] 评估增加 WebSocket 实时遥测；控制命令仍保留明确 ACK。
- [ ] 如确实需要兼容旧上位机，增加 TCP 兼容服务或修改旧上位机适配当前 HTTP API，不同时维护两套运动语义。
- [x] 控制、IMU、队列、看门狗和 Wi-Fi 重连均有诊断计数；高速采样不逐帧打印日志。

## P1：上位机与测试

- [x] 验证 `tools/robot_control_gui.py` 可启动，具有中文界面、异步请求和异常提示。
- [x] 上位机增加左右电机、油门转向、急停、GPIO35、nFAULT、IMU、网络和计数状态。
- [ ] 加入 Xbox/XInput 控制适配，沿用旧工程的按键边沿检测和断开保护思想。
- [ ] 上位机关闭和网络断开保护已完成；手柄接入后再补手柄断开保护。
- [x] 主机侧测试覆盖参数边界、方法/路由冻结、超时/乱序、满队列和急停优先级。
- [x] 硬件测试工具默认禁止执行器运动；只有显式 `--allow-motion --test <stage>` 才允许短时 10% 输出。
- [x] 已建立仅 USB -> I2C/IMU -> GPIO35 -> 舵机 -> 单电机 -> 双电机的分阶段板测流程。
- [ ] 每次修改后执行 `idf.py build`；接板后再执行烧录、串口日志和真机回归。

## P2：后续功能

- [ ] OTA 固件升级及失败回滚。
- [ ] 关键参数、故障记录和有限遥测的 Flash/NVS 持久化，设置写入频率限制。
- [ ] 完整的 IMU 姿态融合；基础板测阶段先不引入复杂算法。
- [ ] 为未来控制算法提供稳定的 `SensorState` 和 `ActuatorCommand` 接口。
- [ ] 如果后续确实采用多块控制板，再实现设备 ID、节点发现、多节点同步和旧工程的多端口管理。

## 当前硬件不具备，暂不照搬

- [ ] 旧 STM32 工程的五体节 Fashion Star UART 舵机总线和蛇形/蠕虫步态。
- [ ] PC0-PC3 四路气味传感器 ADC 采集。
- [ ] RDK/激光雷达物理接口。
- [ ] 强化学习策略部署。

这些功能需要相应硬件和明确系统目标后再启用；当前 PCB 不能仅靠软件直接获得这些能力。
