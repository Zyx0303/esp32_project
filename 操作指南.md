# ESP32-S3 机器人控制器 - 操作指南

## 硬件配置

### 开发板
- **芯片**: ESP32-S3 (QFN56, revision v0.2)
- **型号**: S3-N16R8 (WiFi + BT)
- **Flash**: 2MB | **PSRAM**: 8MB
- **晶振**: 40MHz | **蓝牙**: BLE 5.0

### 引脚定义

| 功能 | GPIO | 说明 |
|------|------|------|
| I2C SCL | 40 | MPU6050 时钟 |
| I2C SDA | 41 | MPU6050 数据 |
| DRV8833 nSLEEP | 48 | 电机驱动睡眠控制 |
| DRV8833 AIN1 | 47 | 电机A 正转 |
| DRV8833 AIN2 | 21 | 电机A PWM |
| DRV8833 BIN1 | 14 | 电机B 正转 |
| DRV8833 BIN2 | 13 | 电机B PWM |
| 舵机 PWM | 45 | 50Hz PWM 输出 |

---

## 一、构建固件

### 环境准备

确保 ESP-IDF 环境变量已设置：

```bash
# Windows PowerShell
$env:IDF_PATH="F:\Espressif\frameworks\esp-idf-v5.3.1"
$env:PATH="F:\Espressif\tools\ninja\1.11.1;F:\Espressif\tools\cmake\3.24.0\bin;" + $env:PATH
$env:MSYSTEM=""
```

### 构建命令

```bash
cd D:\esp32_project
F:\Espressif\python_env\idf5.3_py3.10_env\Scripts\python.exe F:\Espressif\frameworks\esp-idf-v5.3.1\tools\idf.py build
```

或使用简化命令（在项目目录下直接运行）：

```bash
# 先设置环境变量，然后
F:\Espressif\python_env\idf5.3_py3.10_env\Scripts\python.exe F:\Espressif\frameworks\esp-idf-v5.3.1\tools\idf.py build
```

### 完整单行命令

```bash
cd D:\esp32_project; $env:IDF_PATH='F:\Espressif\frameworks\esp-idf-v5.3.1'; $env:PATH='F:\Espressif\tools\ninja\1.11.1;F:\Espressif\tools\cmake\3.24.0\bin;' + $env:PATH; $env:MSYSTEM=''; F:\Espressif\python_env\idf5.3_py3.10_env\Scripts\python.exe F:\Espressif\frameworks\esp-idf-v5.3.1\tools\idf.py build
```

---

## 二、烧录固件

### 进入下载模式（关键步骤）

**必须按住 IO0 开关保持不放，然后按 EN 复位：**

1. **按住 IO0 开关**（SW2）- 保持按住，不要松手
2. **按一下 EN 开关**（SW1）- 复位芯片
3. **继续保持按住 IO0** - 整个烧录过程中都不要松手
4. 烧录完成后才能松开 IO0

### 烧录命令

**必须使用 idf5.3_py3.10_env 环境中的 Python：**

```bash
F:\Espressif\python_env\idf5.3_py3.10_env\Scripts\python.exe -m esptool --chip esp32s3 -p COM11 -b 460800 --before default_reset --after hard_reset write_flash --flash_mode dio --flash_size 2MB --flash_freq 80m 0x0 D:/esp32_project/build/bootloader/bootloader.bin 0x10000 D:/esp32_project/build/esp32_s3.bin 0x8000 D:/esp32_project/build/partition_table/partition-table.bin
```

**注意：**
- 如果 COM 口不是 COM11，需要修改 `-p COM11` 为实际端口号
- 不要直接用 `python -m esptool`，必须用 `F:\Espressif\python_env\idf5.3_py3.10_env\Scripts\python.exe -m esptool`

### 验证烧录成功

烧录成功会显示：
```
Leaving...
Hard resetting via RTS pin...
```

---

## 三、串口监视

### 串口监视

**方法一：使用 idf.py monitor**

```bash
F:\Espressif\python_env\idf5.3_py3.10_env\Scripts\python.exe F:\Espressif\frameworks\esp-idf-v5.3.1\tools\idf.py -p COM11 monitor
```

**方法二：使用其他串口工具**

- **MobaXterm**: Session → Serial → COM11, 115200
- **Arduino IDE**: 串口监视器, 115200

---

## 四、测试命令

连接串口后（115200 波特率），发送以下命令：

| 命令 | 说明 | 示例 |
|------|------|------|
| `help` | 显示所有命令 | `help` |
| `motor <0-100>` | 设置电机速度 | `motor 50` |
| `servo <0-180>` | 设置舵机角度 | `servo 90` |
| `i2c scan` | 扫描 I2C 设备 | `i2c scan` |
| `imu` | 读取 MPU6050 | `imu` |

### 完整测试流程

```bash
# 连接串口后依次发送：

help        # 查看帮助
motor 50    # 电机 50% 速度
motor 0     # 电机停止
servo 90    # 舵机转到 90 度
servo 0     # 舵机归零
i2c scan    # 扫描 I2C 设备
imu         # 读取 MPU6050
```

---

## 五、Python 串口测试脚本

如果需要用 Python 通过串口发送命令：

```python
import serial
import time

ser = serial.Serial('COM11', 115200, timeout=1)
time.sleep(1)

# 发送命令
ser.write(b'motor 50\n')
time.sleep(1)

ser.write(b'servo 90\n')
time.sleep(1)

# 读取响应
response = ser.read(ser.in_waiting)
print(response.decode('utf-8', errors='replace'))

ser.close()
```

---

## 六、已知问题

### I2C 扫描找不到设备

**原因**: MPU6050 模块需要 4.7K 上拉电阻连接 SDA/SCL 到 3.3V

**解决方法**: 在 SDA 和 SCL 上各接一个 4.7K 电阻到 3.3V

---

## 七、项目文件结构

```
esp32_project/
├── main/
│   ├── app_main.c      # 主程序（测试固件）
│   └── CMakeLists.txt  # 主组件配置
├── esp32_s3/           # 板级驱动组件
│   ├── board_pins.c/h
│   ├── drv8833.c/h
│   ├── servo_pwm.c/h
│   ├── mpu6050.c/h
│   ├── flash_storage.c/h
│   ├── ble_control.c/h
│   └── CMakeLists.txt
├── build/              # 构建输出目录
│   ├── bootloader/bootloader.bin
│   ├── esp32_s3.bin
│   └── partition_table/partition-table.bin
├── partitions.csv
├── sdkconfig
└── README.md
```

---

## 八、构建时间

- 最后构建: 2026-05-26
- ESP-IDF: v5.3.1-dirty
