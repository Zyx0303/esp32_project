# ESP32-S3 机器人操作指南

## 1. 编译

编译只需要完整的 ESP-IDF 环境，不需要连接开发板：

```powershell
cd D:\esp32_project
idf.py set-target esp32s3
idf.py build
```

## 2. 烧录

连接开发板，确认设备管理器中的实际 COM 端口，然后执行：

```powershell
idf.py -p COM7 flash monitor
```

板载 CH340 的 DTR/RTS 已接入自动下载电路，正常情况下不需要一直按住 IO0。若自动下载失败，再按住 BOOT/IO0、点按 EN 后重试。

## 3. Wi-Fi 控制

固件启动后：

1. 手机或电脑连接热点 `ESP32-Robot`。
2. 输入密码 `esp32robot`。
3. 浏览器打开 `http://192.168.4.1`。
4. 按住“前进”或“后退”控制电机，松开自动停止；拖动滑块控制舵机。

Wi-Fi 中断或控制指令停止刷新超过 1 秒时，电机会自动停止。

## 4. HTTP 接口

```text
GET http://192.168.4.1/api/status
GET http://192.168.4.1/api/motor?speed=50
GET http://192.168.4.1/api/motor?speed=-50
GET http://192.168.4.1/api/servo?angle=90
GET http://192.168.4.1/api/stop
```

## 5. 串口调试

波特率：115200。

```text
help
motor 50
motor -50
motor 0
servo 90
i2c scan
imu
```

## 6. 当前引脚

| 功能 | GPIO |
|---|---:|
| I2C SCL | 11 |
| I2C SDA | 12 |
| MPU6050 INT | 8 |
| DRV8833 nSLEEP | 3 |
| DRV8833 nFAULT | 4 |
| DRV8833 AIN1 | 46（仅输入，必须改线） |
| DRV8833 AIN2 | 9 |
| DRV8833 BIN1 | 5 |
| DRV8833 BIN2 | 6 |
| 舵机 PWM | 45 |

## 7. 重要限制

GPIO46 不能输出。固件会因此禁用整个 DRV8833，并保持驱动器休眠。完成硬件飞线并修改 `board_pins.h` 后才能实际驱动电机。
